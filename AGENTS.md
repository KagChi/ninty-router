# AGENTS.md — ninty-router

Local AI router: OpenAI-compatible endpoint on localhost, tiered provider fallback,
token-saver compression. Rust (axum) + Solid-Start, single binary in prod.

All execution plans and agent behaviour rules live in `.opencode/plans/`.
Read `.opencode/plans/AGENTS.md` first, then `00-architecture.md`, then the current
milestone file (01–10, in order).
